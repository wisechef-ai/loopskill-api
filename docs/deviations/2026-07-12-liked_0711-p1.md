# liked_0711 P1 deviations

- `ruff format app/` reformatted the pre-existing IPv4 regex in
  `app/services/connector_validation.py`. This is a formatting-only change
  required for the mandated formatter and does not alter P1 behavior.

## Opus review fixes (2026-07-12, post-Codex)
- CI caught 3 failures Codex's local run missed (Codex validated against a subset):
  1. `like.py` MCP tool tripped the secfix_1905-B authz audit (no `authz.can_*` in file).
     FIX: added a real read-authz gate at the shared chokepoint `set_liked_artifact`
     (`authz.can_read_skill` — cannot like a private skill you may not read). New
     `LikedArtifactForbiddenError` → 403 (HTTP) / `{error:forbidden}` (MCP). This
     protects BOTH surfaces via one gate and is stronger than the audit required.
  2/3. `app/mcp/server.py` hit 605 lines, over the NEVER-waived 600 god-object cap
     (main was already at 596). FIX: extracted `recipes_like` dispatch into
     `dispatch_library()` in like.py, mirroring the existing `dispatch_composite_loop`
     pattern + reusing the shared `_NOT_HANDLED` sentinel. server.py back to 600.
- Added `test_like_private_skill_of_another_user_is_forbidden` (RED-proofed: neutralizing
  the gate flips 403→200 and the test fails, confirming the gate is load-bearing).
- NOTE for reviewer: server.py at exactly 600 has ZERO headroom — the dispatcher is a
  god-object smell that pre-dates this sprint; a future phase should split it properly.
