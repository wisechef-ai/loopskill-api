# recipes/plan-for-goal/recipe.yaml
# Pure-prompt skill — no runtime dependencies, no binaries, no services.

runtime:
  binaries: []
  services: []
  env:
    optional: []
  cron: []
  compatibility:
    os: [linux, macos, windows]
    arch: [x86_64, arm64]
    ram_gb:
      minimum: 0
      recommended: 0
    disk_gb: 0
    network: optional
