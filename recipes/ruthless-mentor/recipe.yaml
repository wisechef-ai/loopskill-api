# recipes/ruthless-mentor/recipe.yaml
# Pure-prompt skill — no runtime dependencies, no binaries, no services.
# Activated by trigger phrases in the host agent's conversation.

runtime:
  binaries: []
  services: []
  env:
    optional: []
  cron: []
  compatibility:
    os: [linux, macos, windows]
    arch: [x86_64, arm64]
    ram_gb:
      minimum: 0
      recommended: 0
    disk_gb: 0
    network: optional
