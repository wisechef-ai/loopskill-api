# Internal

This directory contains closed-source-only artifacts not intended for public distribution.
If the repository is flipped open-source, run `git rm -r internal/` first, or rely on `.gitattributes export-ignore`.
