"""recipes runtime extension (Phase F).

Per-skill install machinery: schema/validator (F.1), per-OS adapters (F.2),
service provisioners (F.3), cron registrars (F.4), health orchestration (F.5),
atomic rollback (F.6), arch-aware probe (F.7), and staleness pipeline (F.8).
"""
